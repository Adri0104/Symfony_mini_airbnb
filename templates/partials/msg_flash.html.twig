{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for msg in messages %}
                <p>{{ msg | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}
