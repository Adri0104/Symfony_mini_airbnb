{% extends 'base.html.twig' %}

{% block title %}
    Création d'une annonce
{% endblock %}

{#{% form_theme form _self %}#}

{% block body %}
    <div class="container">
        <h1>Créer uune nouvelle annonce</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <p>
                <input type="submit" class="btn btn-primary" value="Créer"> | <a href="{{ path('index.ads') }}">Cancel</a>
            </p>
        {{ form_end(form) }}
    </div>
{% endblock %}
{#

{% block form_row %}
#}
{#    {{ dump() }}#}{#

    <div class="form-group">
        {{ form_label(form) }}
        {{ form_widget(form) }}
    </div>
{% endblock %}

{% block text_widget %}
    <input type="text" name="{{ full_name }}" id="{{ id }}" class="form-control">
{% endblock %}
#}
